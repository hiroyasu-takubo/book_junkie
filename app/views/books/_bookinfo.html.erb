<div class="col-md-4">
  <%= image_tag book.image if book.image? %>
</div>
<div class="col-md-8">
  <p>タイトル：<%= book.title %></p>
  <p>著者：<%= book.author %></p>
  <p>出版社：<%= book.publisher %></p>
</div>
